<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_graph"
    app:startDestination="@id/products"
    >

    <fragment
        android:id="@+id/products"
        android:name="com.example.technotestvk.ProductPage"
        android:label="fragment_products"
        tools:layout="@layout/fragment_products"
        >
        <action
            android:id="@+id/action_products_to_productPageFragment2"
            app:destination="@id/productPageFragment"
            />
    </fragment>
    <fragment
        android:id="@+id/productPageFragment"
        android:name="com.example.technotestvk.ProductPageFragment"
        android:label="fragment_product_page"
        tools:layout="@layout/fragment_product_page"
        >
        <argument
            android:name="id"
            app:argType="integer"
            />
    </fragment>
</navigation>